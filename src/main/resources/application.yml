spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: briancode
    url: jdbc:postgresql://localhost:5332/brian
    password: password

  application:
    name: spring-boot

  jpa:
    properties:
      hibernate:
        format_sql: 'true'
    hibernate:
      ddl-auto: create-drop
    show-sql: 'true'
